# The Setlist - Docker Compose
# Usage: docker-compose up --build

version: '3.8'

services:
  setlist:
    build: .
    container_name: the-setlist
    ports:
      - "8501:8501"
    volumes:
      # Mount secrets file (create from secrets.toml.example)
      - ./.streamlit/secrets.toml:/app/.streamlit/secrets.toml:ro
    environment:
      - STREAMLIT_SERVER_HEADLESS=true
      - STREAMLIT_BROWSER_GATHER_USAGE_STATS=false
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8501/_stcore/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

# For production with environment variables instead of secrets file:
# services:
#   setlist:
#     build: .
#     ports:
#       - "8501:8501"
#     environment:
#       - SUPABASE_URL=${SUPABASE_URL}
#       - SUPABASE_KEY=${SUPABASE_KEY}
#       - SPOTIFY_CLIENT_ID=${SPOTIFY_CLIENT_ID}
#       - SPOTIFY_CLIENT_SECRET=${SPOTIFY_CLIENT_SECRET}
#       - SPOTIFY_REDIRECT_URI=${SPOTIFY_REDIRECT_URI}
#       - TICKETMASTER_API_KEY=${TICKETMASTER_API_KEY}
#       - SEATGEEK_CLIENT_ID=${SEATGEEK_CLIENT_ID}
